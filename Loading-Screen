-- Xeno's Pet Spawner UI (Base UI Layout with Cleanup)

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Destroy old UI if it exists
if playerGui:FindFirstChild("XenosPetSpawnerUI") then
    playerGui:FindFirstChild("XenosPetSpawnerUI"):Destroy()
end

-- Main UI ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "XenosPetSpawnerUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 400, 0, 300)
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
mainFrame.BackgroundTransparency = 0.2
mainFrame.BorderSizePixel = 0
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Parent = screenGui

-- Rounded corners
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 16)
corner.Parent = mainFrame

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 16)
titleCorner.Parent = titleBar

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 1, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "✨ Xeno's Pet Spawner 🐾"
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextScaled = true
titleLabel.Parent = titleBar

-- Pet Selector Title
local selectorTitle = Instance.new("TextLabel")
selectorTitle.Position = UDim2.new(0, 20, 0, 50)
selectorTitle.Size = UDim2.new(0, 200, 0, 20)
selectorTitle.BackgroundTransparency = 1
selectorTitle.Text = "Pet Selector"
selectorTitle.Font = Enum.Font.GothamBold
selectorTitle.TextColor3 = Color3.fromRGB(200, 200, 200)
selectorTitle.TextSize = 16
selectorTitle.TextXAlignment = Enum.TextXAlignment.Left
selectorTitle.Parent = mainFrame

-- Pet Dropdown
local dropdown = Instance.new("TextButton")
dropdown.Name = "PetDropdown"
dropdown.Position = UDim2.new(0, 20, 0, 75)
dropdown.Size = UDim2.new(0, 360, 0, 30)
dropdown.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
dropdown.Text = "Select a Pet"
dropdown.Font = Enum.Font.Gotham
dropdown.TextColor3 = Color3.fromRGB(255, 255, 255)
dropdown.TextSize = 14

dropdown.AutoButtonColor = true
dropdown.Parent = mainFrame

local dropdownCorner = Instance.new("UICorner")
dropdownCorner.CornerRadius = UDim.new(0, 8)
dropdownCorner.Parent = dropdown

-- Weight Title
local weightTitle = Instance.new("TextLabel")
weightTitle.Position = UDim2.new(0, 20, 0, 120)
weightTitle.Size = UDim2.new(0, 180, 0, 20)
weightTitle.BackgroundTransparency = 1
weightTitle.Text = "Weight"
weightTitle.Font = Enum.Font.GothamBold
weightTitle.TextColor3 = Color3.fromRGB(200, 200, 200)
weightTitle.TextSize = 16
weightTitle.TextXAlignment = Enum.TextXAlignment.Left
weightTitle.Parent = mainFrame

-- Age Title
local ageTitle = Instance.new("TextLabel")
ageTitle.Position = UDim2.new(0, 210, 0, 120)
ageTitle.Size = UDim2.new(0, 180, 0, 20)
ageTitle.BackgroundTransparency = 1
ageTitle.Text = "Age"
ageTitle.Font = Enum.Font.GothamBold
ageTitle.TextColor3 = Color3.fromRGB(200, 200, 200)
ageTitle.TextSize = 16
ageTitle.TextXAlignment = Enum.TextXAlignment.Left
ageTitle.Parent = mainFrame

-- Weight Input
local weightBox = Instance.new("TextBox")
weightBox.Position = UDim2.new(0, 20, 0, 145)
weightBox.Size = UDim2.new(0, 170, 0, 30)
weightBox.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
weightBox.PlaceholderText = "Enter weight"
weightBox.Font = Enum.Font.Gotham
weightBox.TextColor3 = Color3.fromRGB(255, 255, 255)
weightBox.TextSize = 14
weightBox.Parent = mainFrame

local weightCorner = Instance.new("UICorner")
weightCorner.CornerRadius = UDim.new(0, 8)
weightCorner.Parent = weightBox

-- Age Input
local ageBox = Instance.new("TextBox")
ageBox.Position = UDim2.new(0, 210, 0, 145)
ageBox.Size = UDim2.new(0, 170, 0, 30)
ageBox.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
ageBox.PlaceholderText = "Enter age"
ageBox.Font = Enum.Font.Gotham
ageBox.TextColor3 = Color3.fromRGB(255, 255, 255)
ageBox.TextSize = 14
ageBox.Parent = mainFrame

local ageCorner = Instance.new("UICorner")
ageCorner.CornerRadius = UDim.new(0, 8)
ageCorner.Parent = ageBox

-- Footer
local footer = Instance.new("TextLabel")
footer.Size = UDim2.new(1, 0, 0, 25)
footer.Position = UDim2.new(0, 0, 1, -25)
footer.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
footer.Text = "Made by XenoScripts | Premium"
footer.TextColor3 = Color3.fromRGB(160, 160, 160)
footer.Font = Enum.Font.GothamSemibold
footer.TextSize = 12
footer.BorderSizePixel = 0
footer.Parent = mainFrame

local footerCorner = Instance.new("UICorner")
footerCorner.CornerRadius = UDim.new(0, 8)
footerCorner.Parent = footer
