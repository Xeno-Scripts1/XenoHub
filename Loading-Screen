-- Xeno's Pet Spawner UI (Updated with Fixes and Styling)

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Clear previous UI
if playerGui:FindFirstChild("XenosPetSpawnerUI") then
    playerGui:FindFirstChild("XenosPetSpawnerUI"):Destroy()
end

-- ScreenGui setup
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "XenosPetSpawnerUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Main frame
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 400, 0, 300)
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BackgroundTransparency = 0.2
mainFrame.BorderSizePixel = 0
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Parent = screenGui
mainFrame.ClipsDescendants = true
mainFrame.Name = "MainFrame"

-- UICorner for rounded edges
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 16)
mainCorner.Parent = mainFrame

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 12)
titleCorner.Parent = titleBar

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 1, 0)
title.BackgroundTransparency = 1
title.Text = "🐾 Xeno's Pet Spawner 🐾"
title.Font = Enum.Font.FredokaOne
title.TextColor3 = Color3.fromRGB(0, 255, 150)
title.TextScaled = true
title.Parent = titleBar

-- Pet Selector Title
local petTitle = Instance.new("TextLabel")
petTitle.Size = UDim2.new(1, -40, 0, 20)
petTitle.Position = UDim2.new(0, 20, 0, 60)
petTitle.BackgroundTransparency = 1
petTitle.Text = "🦴 Pet Selector"
petTitle.Font = Enum.Font.GothamBold
petTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
petTitle.TextScaled = true
petTitle.TextXAlignment = Enum.TextXAlignment.Left
petTitle.Parent = mainFrame

-- Dropdown (placeholder)
local petDropdown = Instance.new("TextButton")
petDropdown.Size = UDim2.new(1, -40, 0, 30)
petDropdown.Position = UDim2.new(0, 20, 0, 90)
petDropdown.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
petDropdown.TextColor3 = Color3.fromRGB(200, 200, 200)
petDropdown.Text = "Select a Pet"
petDropdown.Font = Enum.Font.Gotham
petDropdown.TextSize = 16
petDropdown.Parent = mainFrame

local dropdownCorner = Instance.new("UICorner")
dropdownCorner.CornerRadius = UDim.new(0, 8)
dropdownCorner.Parent = petDropdown

-- Age and Weight Labels
local ageLabel = Instance.new("TextLabel")
ageLabel.Size = UDim2.new(0.5, -25, 0, 20)
ageLabel.Position = UDim2.new(0, 20, 0, 140)
ageLabel.BackgroundTransparency = 1
ageLabel.Text = "🎂 Age"
ageLabel.Font = Enum.Font.GothamBold
ageLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
ageLabel.TextSize = 14
ageLabel.Parent = mainFrame

local weightLabel = Instance.new("TextLabel")
weightLabel.Size = UDim2.new(0.5, -25, 0, 20)
weightLabel.Position = UDim2.new(0.5, 5, 0, 140)
weightLabel.BackgroundTransparency = 1
weightLabel.Text = "⚖️ Weight"
weightLabel.Font = Enum.Font.GothamBold
weightLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
weightLabel.TextSize = 14
weightLabel.Parent = mainFrame

-- Age and Weight Inputs
local ageInput = Instance.new("TextBox")
ageInput.Size = UDim2.new(0.5, -25, 0, 30)
ageInput.Position = UDim2.new(0, 20, 0, 165)
ageInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ageInput.TextColor3 = Color3.fromRGB(255, 255, 255)
ageInput.PlaceholderText = "Enter Age"
ageInput.Font = Enum.Font.Gotham
ageInput.TextSize = 16
ageInput.Parent = mainFrame

local ageCorner = Instance.new("UICorner")
ageCorner.CornerRadius = UDim.new(0, 8)
ageCorner.Parent = ageInput

local weightInput = Instance.new("TextBox")
weightInput.Size = UDim2.new(0.5, -25, 0, 30)
weightInput.Position = UDim2.new(0.5, 5, 0, 165)
weightInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
weightInput.TextColor3 = Color3.fromRGB(255, 255, 255)
weightInput.PlaceholderText = "Enter Weight"
weightInput.Font = Enum.Font.Gotham
weightInput.TextSize = 16
weightInput.Parent = mainFrame

local weightCorner = Instance.new("UICorner")
weightCorner.CornerRadius = UDim.new(0, 8)
weightCorner.Parent = weightInput

-- Footer
local footer = Instance.new("TextLabel")
footer.Size = UDim2.new(1, 0, 0, 20)
footer.Position = UDim2.new(0, 0, 1, -20)
footer.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
footer.Text = "Made by XenoScripts | Premium"
footer.TextColor3 = Color3.fromRGB(0, 255, 150)
footer.TextSize = 14
footer.Font = Enum.Font.Gotham
footer.Parent = mainFrame

local footerCorner = Instance.new("UICorner")
footerCorner.CornerRadius = UDim.new(0, 10)
footerCorner.Parent = footer
